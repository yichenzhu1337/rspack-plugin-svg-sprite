{
  "name": "svg-sprite-vue-extract-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Vue 3 demo using extract mode with plainSprite and external <use> references",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "rspack-plugin-svg-sprite": "workspace:*"
  },
  "devDependencies": {
    "@rspack/cli": "^1.7.6",
    "@rspack/core": "^1.7.6",
    "vue-loader": "^17.4.2"
  }
}
